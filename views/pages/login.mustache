{{> header }}

<div class="container-fluid d-flex justify-content-center align-items-center login-bg">
  <div style="max-width: 400px; width: 100%;">
    <h1 class="text-center mb-3 text-white">Simul8 Help Files Manager</h1>

    {{#error}}
    <div class="alert alert-danger">{{error}}</div>
    {{/error}}

    <form method="POST" action="/auth/login" class="border p-4 shadow rounded bg-white">
      <div class="form-group">
        <label for="username">Username</label>
        <input id="username" name="username" class="form-control" required>
      </div>

      <div class="form-group mt-3">
        <label for="password">Password</label>
        <input id="password" name="password" type="password" class="form-control" required>
      </div>

      <button class="btn btn-primary w-100 mt-3">Login</button>
    </form>

    <!--<div class="text-center mt-3">
      <p class="text-muted">Don't have an account? <a href="/auth/register">Register here</a></p>
      <p class="text-muted">Forgot your password? <a href="/auth/forgot-password">Reset it</a></p>  
    </div>-->

  </div>
</div>